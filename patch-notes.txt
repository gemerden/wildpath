version 0.1.7:

 - initial version

version 0.2.0

 - object attribute access is now based on dir(), getattr(), setattr(), delattr() instead of direct obj.__dict__ access.

version 0.2.1

 - improves handling of defaults for WildPath.get_in: now replaces missing values in result instead of returning a flat value.